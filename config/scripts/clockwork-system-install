#!/bin/bash
# Wrapper used by clockwork-web (via sudo) to install system-scope unit files.
# Root's Python environment doesn't include the editable clockwork package, so
# we inject the source path explicitly before calling the module.
PYTHONPATH="/path/to/portfolio/util-repos/clockwork/src" exec /usr/bin/python3 -m clockwork install "$@"
